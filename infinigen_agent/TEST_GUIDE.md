# 测试指南

## 方法1: 完整流程测试（生成场景 + 应用颜色 + 渲染）

### 交互式模式（推荐用于测试）

```bash
cd /home/ubuntu/infinigen/infinigen_agent
python src/langchain_agent.py
```

然后输入场景描述，例如：
- `生成一个北欧风格的卧室`
- `生成一个现代风格的客厅`
- `生成一个简约的厨房`

### 命令行模式（快速测试）

```bash
cd /home/ubuntu/infinigen/infinigen_agent

# 基本测试（使用随机 seed，0-10000）
python src/langchain_agent.py "生成一个北欧风格的卧室"

# 使用官方推荐的 seed 0（最快）
python src/langchain_agent.py "生成一个现代风格的客厅" --seed 0

# 指定输出文件夹
python src/langchain_agent.py "生成一个简约的厨房" \
    --output-folder outputs/test_kitchen \
    --seed 42

# 指定超时时间（默认 600 秒）
python src/langchain_agent.py "生成一个温馨的卧室" \
    --timeout 900
```

## 方法2: 使用已有场景文件测试（快速，跳过场景生成）

如果你已经有一个生成的场景文件，可以快速测试颜色应用和渲染：

```bash
cd /home/ubuntu/infinigen/infinigen_agent

# 只渲染图片（不应用颜色）
python continue_from_scene.py /home/ubuntu/infinigen/outputs/interactive_1765333976/scene.blend

# 应用颜色并渲染
python continue_from_scene.py /home/ubuntu/infinigen/outputs/interactive_1765333976/scene.blend "生成一个北欧风格的卧室"
```

## 方法3: 测试自动检测功能

### 测试场景文件生成检测

1. 启动场景生成（在另一个终端）：
```bash
cd /home/ubuntu/infinigen
python -m infinigen_examples.generate_indoors \
    --seed 0 \
    --task coarse \
    --output_folder outputs/test_auto_detect \
    -g ultra_fast_solve.gin singleroom.gin \
    -p compose_indoors.terrain_enabled=False
```

2. 在场景生成过程中或完成后，运行：
```bash
cd /home/ubuntu/infinigen/infinigen_agent
python continue_from_scene.py outputs/test_auto_detect/scene.blend "生成一个北欧风格的卧室"
```

这会测试自动检测场景文件并继续处理的功能。

## 预期输出

### 完整流程测试的输出顺序：

1. **步骤1: 验证用户输入**
   ```
   ✓ 输入验证通过: ...
   ```

2. **步骤2: 并行生成颜色方案和场景**
   ```
   2.1 生成家具颜色方案...
   ✓ 颜色方案生成成功
   
   2.2 生成场景（使用 Infinigen 原生命令）...
   ⚡ 使用超快配置（ultra_fast_solve.gin + singleroom.gin）
   预计时间: 5-10 分钟
   ...
   ✓ 场景生成成功: ...
   📁 场景文件路径: ...
   ```

3. **步骤3: 应用颜色到场景**
   ```
   ✓ 解析到 X 个颜色配置
   正在加载场景文件: ...
   ⚠ 如果场景文件很大，这可能需要几分钟...
   ✓ 成功加载场景: ...
   ✓ 颜色已应用到场景并保存
   ```

4. **步骤4: 渲染场景图片**
   ```
   步骤4: 渲染场景图片...
   ✓ 成功加载场景: ...
   ✓ 图片渲染成功: ...
   ```

5. **完成**
   ```
   ✓ 处理完成！
   场景文件: ...
   渲染图片: ...
   ```

## 检查结果

测试完成后，检查输出：

```bash
# 查看输出文件夹
ls -lh /home/ubuntu/infinigen/outputs/langchain_*/

# 检查场景文件
ls -lh /home/ubuntu/infinigen/outputs/langchain_*/scene.blend

# 检查渲染图片
ls -lh /home/ubuntu/infinigen/outputs/langchain_*/rendered_image.png

# 查看图片（如果有图像查看器）
# xdg-open /home/ubuntu/infinigen/outputs/langchain_*/rendered_image.png
```

## 常见问题

### 1. 场景生成太慢
- 已使用 `ultra_fast_solve.gin`，预计 5-10 分钟
- 如果还是太慢，可以尝试使用 `minimal_furniture.gin`（家具更少）

### 2. 颜色应用失败
- 检查模型输出格式是否正确
- 查看错误信息，可能是颜色解析问题
- 即使颜色应用失败，也会继续渲染（使用原始颜色）

### 3. 渲染失败
- 检查场景文件是否存在且有效
- 检查是否有足够的 GPU/CPU 资源
- 查看错误信息

## 快速测试命令（推荐）

```bash
# 最快测试（使用 seed 0，官方推荐）
cd /home/ubuntu/infinigen/infinigen_agent
python src/langchain_agent.py "生成一个餐厅" --seed 0
```
