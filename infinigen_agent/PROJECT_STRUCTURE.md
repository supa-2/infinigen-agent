# 项目结构说明

## 📁 目录结构

```
infinigen_agent/
├── src/                          # 核心源代码
│   ├── langchain_agent.py        # 主入口（LangChain Agent）
│   ├── agent.py                  # 旧版 Agent（已弃用）
│   ├── scene_generator.py        # 场景生成器
│   ├── scene_renderer.py         # 场景渲染器
│   ├── scene_color_applier.py    # 颜色应用器
│   ├── color_parser.py           # 颜色解析器
│   ├── room_type_detector.py    # 房间类型检测器
│   ├── procedural_furniture_generator.py  # 程序化家具生成器
│   ├── vllm_client.py           # vLLM 客户端
│   └── static_asset_importer.py # 静态资产导入器（已弃用）
│
├── tests/                        # 测试文件
│   ├── test_*.py                # 各种测试脚本
│   └── monitoring/              # 监控相关测试
│
├── tools/                        # 工具脚本
│   ├── monitoring/              # 监控和检查脚本
│   ├── download_*.py           # 下载工具
│   ├── render_*.py             # 渲染工具
│   └── *.py                     # 其他工具
│
├── docs/                         # 文档
│   ├── *.md                     # 详细文档
│   └── archive/                 # 归档文档
│
├── config/                       # 配置文件
│   └── api_config.py            # API 配置
│
├── scripts/                      # Shell 脚本
│   └── *.sh                     # 各种脚本
│
├── continue_from_scene.py       # 从已有场景继续处理的工具
├── README.md                    # 项目说明
├── USAGE.md                     # 使用指南
├── ARCHITECTURE.md              # 架构文档
└── TEST_GUIDE.md               # 测试指南
```

## 🚀 主要入口文件

### 1. `src/langchain_agent.py`（推荐）
**主入口文件**，可以直接运行：
```bash
python src/langchain_agent.py "生成一个北欧风格的卧室"
```

### 2. `continue_from_scene.py`
从已有场景文件继续处理（应用颜色和渲染）：
```bash
python continue_from_scene.py outputs/xxx/scene.blend "生成一个北欧风格的卧室"
```

### 3. `run_agent.py`（旧版，已弃用）
使用旧的 `InfinigenAgent`，不推荐使用。

### 4. `run_langchain_agent.py`（包装脚本，可选）
`langchain_agent.py` 的包装脚本，功能相同。

## 📝 文档说明

### 根目录文档（重要）
- `README.md` - 项目总览
- `USAGE.md` - 使用指南
- `ARCHITECTURE.md` - 架构说明
- `TEST_GUIDE.md` - 测试指南

### docs/ 目录
包含详细的开发文档、历史记录、问题解决方案等。

## 🧪 测试文件

所有测试文件都在 `tests/` 目录下：
- `test_*.py` - 各种功能测试
- `monitoring/` - 监控相关测试

## 🛠️ 工具脚本

所有工具脚本都在 `tools/` 目录下：
- `monitoring/` - 监控和检查脚本
- 其他工具脚本

## 📦 源代码

所有核心代码都在 `src/` 目录下：
- `langchain_agent.py` - 主 Agent（推荐使用）
- `agent.py` - 旧版 Agent（已弃用）
- 其他模块

