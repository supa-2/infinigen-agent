# 双模式功能使用说明

## 功能概述

Web 界面现在支持两种场景生成模式：

1. **📋 模板模式（Template Mode）**
   - 从模板池挑选预生成的场景模板
   - 自动应用颜色方案
   - 快速生成（2-3分钟）
   - 高质量 Cycles 渲染

2. **🏗️  生成模式（Generate Mode）**
   - 生成全新的场景布局
   - 快速预览（<1秒，Eevee引擎）
   - 用户确认后可选精修渲染
   - 总耗时：3-8分钟（生成）+ 预览（<1秒）+ 可选精修（~1分钟）

## 使用方法

### 1. 选择模式

在聊天界面输入框上方有两个按钮：
- **📋 模板模式（快速，2-3分钟）**：使用预生成模板
- **🏗️  生成模式（3-8分钟，预览+确认）**：生成全新场景

### 2. 模板模式流程

1. 点击"📋 模板模式"按钮
2. 输入场景描述，例如："生成一个红色调的北欧卧室"
3. 系统自动：
   - 从模板池检索匹配的模板
   - 生成颜色方案
   - 应用颜色到场景
   - 进行 Cycles 高质量渲染
4. 等待 2-3 分钟，完成后显示结果

### 3. 生成模式流程

1. 点击"🏗️  生成模式"按钮
2. 输入场景描述，例如："生成一个现代简约风格的客厅"
3. 系统生成场景（3-8分钟）
4. 生成完成后，显示快速预览图（<1秒）
5. 用户确认：
   - **确认并精修渲染**：进行 Cycles 高质量渲染（约1分钟）
   - **仅下载场景文件**：直接下载场景文件，不进行精修渲染

## API 接口

### POST /api/generate

**请求体**：
```json
{
  "request": "生成一个北欧风格的卧室",
  "seed": null,
  "mode": "template",  // "template" 或 "generate"
  "auto_confirm": false  // 仅用于 generate 模式
}
```

**响应**：
```json
{
  "task_id": "uuid",
  "status": "pending",
  "message": "任务已创建（模板模式），正在生成场景...",
  "mode": "template"
}
```

### POST /api/task/<task_id>/confirm

确认并精修渲染（仅用于生成模式）

**响应**：
```json
{
  "task_id": "uuid",
  "status": "rendering",
  "message": "已确认，正在精修渲染..."
}
```

### GET /api/task/<task_id>/preview

获取预览图片（仅生成模式）

### GET /api/task/<task_id>/status

**响应（生成模式预览状态）**：
```json
{
  "task_id": "uuid",
  "status": "completed",
  "preview_image": "/path/to/preview.png",
  "needs_confirmation": true,
  "mode": "generate"
}
```

## 性能对比

| 模式 | 场景生成 | 预览 | 精修渲染 | 总耗时 |
|------|---------|------|---------|--------|
| **模板模式** | <1秒（模板） | - | ~1分钟 | **2-3分钟** |
| **生成模式（仅预览）** | 3-8分钟 | <1秒 | - | **3-8分钟** |
| **生成模式（完整）** | 3-8分钟 | <1秒 | ~1分钟 | **4-9分钟** |

## 注意事项

1. **模板池要求**：模板模式需要预先生成模板池
   ```bash
   cd /home/ubuntu/infinigen/infinigen_agent
   python tools/generate_template_pool.py --all
   ```

2. **生成模式预览**：预览使用 Eevee 引擎，质量较低但速度很快，主要用于查看布局

3. **精修渲染**：精修渲染使用 Cycles 引擎，质量高但耗时较长

## 故障排除

### 模板模式找不到模板

如果提示"未找到模板"，请先运行：
```bash
python tools/generate_template_pool.py --all
```

### 预览图片不显示

检查后端日志，确认预览渲染是否成功。预览渲染失败不会影响场景生成。

### 精修渲染失败

如果精修渲染失败，场景文件和预览图仍然可用，可以下载使用。

