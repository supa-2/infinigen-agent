# 前端部署说明

## 两种前端部署方式

### ⭐ 方式 1：独立的 Vite React 项目（推荐）

**优点**：
- 完整的 React 功能和现代开发工具
- 支持所有图标和 UI 组件
- 热重载开发体验
- 包含完整的双模式功能

**使用方法**：

#### 步骤 1：启动后端
```bash
cd /home/ubuntu/infinigen/infinigen_web
python app.py
# 后端运行在 http://localhost:5000
```

#### 步骤 2：启动前端（新终端）
```bash
cd /home/ubuntu/infinigen/infinigen_web/infinigen-web-frontend
npm run dev
# 前端运行在 http://localhost:5173
```

#### 步骤 3：访问
打开浏览器访问：**http://localhost:5173**

---

### 方式 2：Flask 模板（简单但功能受限）

**注意**：Flask 模板使用 CDN 版本的 React，双模式功能已更新但图标库可能受限。

**使用方法**：
```bash
cd /home/ubuntu/infinigen/infinigen_web
python app.py
# 访问 http://localhost:5000
```

---

## 快速启动脚本

### 启动后端（终端 1）
```bash
cd /home/ubuntu/infinigen/infinigen_web
./start_server.sh
```

### 启动前端（终端 2）
```bash
cd /home/ubuntu/infinigen/infinigen_web/infinigen-web-frontend
./start_dev.sh
# 或者
npm run dev
```

---

## 功能说明

### 双模式功能（已集成）✅

1. **📋 模板模式**（快速，2-3分钟）
   - 从模板池检索预生成模板
   - 自动应用颜色
   - Cycles 高质量渲染

2. **🏗️  生成模式**（3-8分钟 + 预览 + 可选精修）
   - 生成全新场景
   - Eevee 快速预览（<1秒）
   - 用户确认后可选 Cycles 精修渲染

### 界面功能

- ✅ 模式选择按钮（模板模式 / 生成模式）
- ✅ 实时进度显示
- ✅ 预览图片展示（生成模式）
- ✅ 确认按钮（生成模式精修渲染）
- ✅ 下载场景文件和渲染图片

---

## API 配置

前端会自动检测 API 地址：
- **开发环境**（localhost）：使用 `http://localhost:5000/api`
- **生产环境**：根据当前域名自动配置

如需手动配置，编辑：
- `infinigen-web-frontend/src/App.jsx` 中的 `API_BASE_URL`

---

## 故障排除

### 前端无法连接后端

1. 检查后端是否运行：`curl http://localhost:5000/api/health`
2. 检查 CORS 设置（后端已启用 CORS）
3. 检查浏览器控制台错误

### 图标不显示

如果使用 Flask 模板方式，图标可能无法正常显示。请使用 Vite 项目方式（推荐）。

### 端口冲突

- 后端默认端口：5000（可在 `app.py` 修改）
- 前端默认端口：5173（Vite 会自动选择可用端口）

---

## 更新记录

✅ 已更新 `infinigen-web-frontend/src/App.jsx` 包含完整的双模式功能

